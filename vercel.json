{
  "installCommand": "npm install",
  "buildCommand": "npm run server:build && mkdir dist/api && mv dist/http.js dist/api/http.js && touch index.html",
  "outputDirectory": "dist",
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        { "key": "Access-Control-Allow-Origin", "value": "*" }
      ]
    }
  ]
}
