{
  "installCommand": "npm install",
  "buildCommand": "npm run server:build && mkdir dist/api && mv dist/http.js dist/api/http.js && ls -l && ls -l dist && ls -l dist/api",
  "outputDirectory": "dist",
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        { "key": "Access-Control-Allow-Origin", "value": "*" }
      ]
    }
  ]
}
