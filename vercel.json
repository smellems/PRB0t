{
  "installCommand": "npm install",
  "buildCommand": "npm run server:build && touch test.html",
  "outputDirectory": "dist",
  "builds": [
    {
      "src": "http.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "http.js",
      "headers": { "Access-Control-Allow-Origin": "*" }
    }
  ]
}
