{
  "version": 2,
  "installCommand": "npm install",
  "buildCommand": "npm run server:build",
  "outputDirectory": ".",
  "builds": [
    {
      "src": "dist/http.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "headers": { "Access-Control-Allow-Origin": "*" }
    }
  ]
}
