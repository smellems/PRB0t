{
  "installCommand": "npm install",
  "buildCommand": "npm run server:build && mkdir api && mv dist/http.js api/http.js",
  "outputDirectory": ".",
  "functions": {
    "api/http.js": {
      "maxDuration": 30
    }
  },
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        { "key": "Access-Control-Allow-Origin", "value": "*" }
      ]
    }
  ]
}
