{
  "installCommand": "npm install",
  "buildCommand": "npm run server:build && mv -p dist/http.js api http.js",
  "outputDirectory": "api",
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        { "key": "Access-Control-Allow-Origin", "value": "*" }
      ]
    }
  ]
}
